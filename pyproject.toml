[dependency-groups]
dev = [
    "pytest>=9.1,<10",
    "ruff>=0.15,<0.17",
]

[tool.ruff]
target-version = "py314"
line-length = 88
extend-exclude = [
    "inventory.py",
    "inventory_v2.py",
    "inventory_v3.py",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
